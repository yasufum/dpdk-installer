---
# spp

# Do not edit understanding risk
dpdk_ver: "{{ dpdk_default_ver }}"
dpdk_target: "{{ dpdk_default_target }}"
dpdk_repo: "{{ dpdk_default_repo }}"
dpdk_home: "{{ dpdk_default_home }}"
dpdk_dir: "{{ dpdk_default_dir }}"

spp_repo: "http://dpdk.org/git/apps/spp"
spp_dir: "{{ dpdk_home }}/spp"
spp_ver: "{{ dpdk_ver }}"
